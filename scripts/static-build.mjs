import { mkdirSync, writeFileSync, copyFileSync, existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

const content = JSON.parse(readFileSync('src/siteContent.json', 'utf8'));
const dist = 'dist';
const siteUrl = (process.env.SITE_URL || process.env.VERCEL_URL || 'https://volponi.com.br').replace(/\/$/, '').replace(/^([^h])/, 'https://$1');
const briefMail = `mailto:${content.email}?subject=Leitura%20de%20campo%20VOLPONI`;

const css = readFileSync('src/App.css', 'utf8').replace(/^#root[^}]+}\n\n/, '').replace(/\.volponi-site /g, '').replace(/\.volponi-site/g, 'body');
const js = `const root=document.documentElement;const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;addEventListener('pointermove',e=>{root.style.setProperty('--mx',e.clientX+'px');root.style.setProperty('--my',e.clientY+'px')},{passive:true});document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{const target=document.querySelector(link.getAttribute('href'));if(!target)return;event.preventDefault();target.scrollIntoView({behavior:reduced?'auto':'smooth',block:'start'})}));const y=document.querySelector('#year');if(y)y.textContent=new Date().getFullYear();`;

const list = (items, render) => items.map(render).join('');
const jsonLd = (path, title) => JSON.stringify({ '@context': 'https://schema.org', '@graph': [
  { '@type': 'WebSite', '@id': `${siteUrl}/#website`, name: content.brand, url: `${siteUrl}/`, inLanguage: 'pt-BR' },
  { '@type': 'Person', '@id': `${siteUrl}/#person`, name: 'Lorenza Volponi', alternateName: content.brand, url: siteUrl, sameAs: [content.github, content.linkedin], knowsAbout: ['inteligência artificial', 'estratégia simbólica', 'automação', 'arquitetura narrativa', 'interfaces digitais'] },
  { '@type': 'ProfessionalService', '@id': `${siteUrl}/#service`, name: 'VOLPONI Intelligence Studio', url: siteUrl, email: content.email, areaServed: 'BR', founder: { '@id': `${siteUrl}/#person` }, serviceType: content.services.map(([name]) => name) },
  { '@type': 'WebPage', name: title, url: `${siteUrl}${path === '/' ? '/' : path}`, isPartOf: { '@id': `${siteUrl}/#website` }, about: { '@id': `${siteUrl}/#service` } },
  { '@type': 'FAQPage', mainEntity: content.faq.map(([name, text]) => ({ '@type': 'Question', name, acceptedAnswer: { '@type': 'Answer', text } })) },
] });

function page(path, title) {
  const canonical = `${siteUrl}${path === '/' ? '/' : path}`;
  return `<!doctype html><html lang="pt-BR"><head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width, initial-scale=1.0"/><title>${title}</title><meta name="description" content="${content.tagline} Um campo digital para transformar ambiguidade em forma, prova e ação."/><meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1"/><meta name="theme-color" content="#040407"/><link rel="canonical" href="${canonical}"/><link rel="icon" href="/favicon.ico"/><link rel="preconnect" href="https://fonts.googleapis.com"/><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/><link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600;700&family=Inter:wght@300;400;500;600;800&display=swap" rel="stylesheet"/><link rel="stylesheet" href="/assets/site.css"/><meta property="og:title" content="${title}"/><meta property="og:description" content="${content.tagline}"/><meta property="og:type" content="website"/><meta property="og:url" content="${canonical}"/><meta property="og:image" content="${siteUrl}/og-volponi.svg"/><meta name="twitter:card" content="summary_large_image"/><script type="application/ld+json">${jsonLd(path, title)}</script></head><body><div class="ambient-noise" aria-hidden="true"></div><header class="site-nav"><a class="brand-mark" href="#home">${content.brand}</a><nav aria-label="Navegação principal">${list(content.nav, ([label, href]) => `<a href="${href}">${label}</a>`)}</nav><a class="nav-button" href="#contato">Solicitar leitura</a></header><main><section id="home" class="hero-section"><div class="hero-copy"><p class="eyebrow">AI • estratégia • presença</p><h1>Inteligência em estado raro.</h1><p class="lead">${content.tagline} Um campo digital para transformar ambiguidade em forma, prova e ação.</p><div class="actions"><a class="button primary" href="#contato">Começar travessia</a><a class="button" href="#laboratorio">Ver laboratório</a></div><div class="proof-strip">${list(content.proof, ([value, label]) => `<article><strong>${value}</strong><span>${label}</span></article>`)}</div></div><aside class="visual-orb"><img src="${content.image}" alt="Identidade visual AIX8C e VOLPONI"/></aside></section><section id="manifesto" class="section-shell manifesto-section"><p class="eyebrow">manifesto</p><h2>Menos vitrine. Mais campo de decisão.</h2><div class="manifesto-grid">${list(content.manifesto, (item) => `<article>${item}</article>`)}</div></section><section id="campo" class="section-shell"><p class="eyebrow">arquitetura do campo</p><h2>Uma presença construída em camadas.</h2><div class="layer-grid">${list(content.fieldLayers, ([title, text]) => `<article class="surface-card"><h3>${title}</h3><p>${text}</p></article>`)}</div></section><section id="laboratorio" class="section-shell"><p class="eyebrow">laboratório público</p><h2>Código como prova. Interface como memória.</h2><div class="project-grid">${list(content.projects, ([name, text, tag]) => `<article class="surface-card"><small>${tag}</small><h3>${name}</h3><p>${text}</p><a href="${content.github}/${name}" target="_blank" rel="noreferrer">Ver no GitHub →</a></article>`)}</div></section><section id="servicos" class="section-shell"><p class="eyebrow">serviços</p><h2>O que construímos.</h2><div class="service-grid">${list(content.services, ([title, text]) => `<article class="surface-card"><h3>${title}</h3><p>${text}</p></article>`)}</div></section><section id="metodo" class="section-shell"><p class="eyebrow">método</p><h2>Da ideia bruta ao sistema vivo.</h2><div class="method-grid">${list(content.method, ([number, title, text]) => `<article class="method-card"><small>${number}</small><h3>${title}</h3><p>${text}</p></article>`)}</div></section><section class="section-shell"><p class="eyebrow">transformações</p><h2>O antes e depois que importa.</h2><div class="case-grid">${list(content.cases, ([before, after, text]) => `<article class="surface-card"><small>${before}</small><h3>${after}</h3><p>${text}</p></article>`)}</div></section><section id="sobre" class="section-shell split-section"><div><p class="eyebrow">sobre</p><h2>Lorenza Volponi.</h2></div><div><p class="lead">Uma presença dedicada à intersecção entre inteligência artificial, linguagem, design, estratégia e sistemas. A proposta é transformar complexidade em forma, beleza em direção e IA em execução concreta.</p><div class="actions"><a class="button" href="${content.github}" target="_blank" rel="noreferrer">GitHub</a><a class="button" href="#contato">Conversar</a></div></div></section><section class="section-shell"><p class="eyebrow">perguntas</p><h2>Clareza antes do briefing.</h2><div class="faq-list">${list(content.faq, ([question, answer]) => `<details class="faq-item"><summary>${question}</summary><p>${answer}</p></details>`)}</div></section><section id="contato" class="section-shell split-section contact-section"><div><p class="eyebrow">acesso</p><h2>Solicite uma leitura de campo.</h2><p class="lead">Envie o contexto do projeto, a tensão atual e o tipo de transformação desejada. A resposta começa pela direção, não pelo excesso.</p></div><aside class="contact-panel"><a href="mailto:${content.email}">${content.email}</a><a href="${content.github}" target="_blank" rel="noreferrer">github.com/LorenzaVolponi</a><a href="${content.linkedin}" target="_blank" rel="noreferrer">linkedin.com/in/lorenzavolponi</a><a class="button primary" href="${briefMail}">Enviar briefing</a></aside></section></main><footer class="site-footer"><strong>${content.brand}</strong><span>© <span id="year"></span> Lorenza Volponi. Inteligência, estratégia e presença digital.</span></footer><script src="/assets/app.js" defer></script></body></html>`;
}

mkdirSync(dist, { recursive: true });
mkdirSync(join(dist, 'assets'), { recursive: true });
writeFileSync(join(dist, 'assets/site.css'), css);
writeFileSync(join(dist, 'assets/app.js'), js);
for (const [path, title] of content.routes) {
  const dir = path === '/' ? dist : join(dist, path.slice(1));
  mkdirSync(dir, { recursive: true });
  writeFileSync(join(dir, 'index.html'), page(path, title));
}
writeFileSync(join(dist, 'sitemap.xml'), `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${content.routes.map(([path]) => `<url><loc>${siteUrl}${path === '/' ? '/' : path}</loc></url>`).join('')}</urlset>`);
writeFileSync(join(dist, 'robots.txt'), `User-agent: *\nAllow: /\nSitemap: ${siteUrl}/sitemap.xml\n`);
writeFileSync(join(dist, 'llms.txt'), `# ${content.brand}\n\n${content.tagline}\n`);
writeFileSync(join(dist, 'humans.txt'), `${content.brand} / Lorenza Volponi\nDesign, IA, estratégia e código.\n`);
writeFileSync(join(dist, 'og-volponi.svg'), `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630"><rect width="1200" height="630" fill="#040407"/><circle cx="820" cy="250" r="260" fill="#7a0f1b" opacity=".55"/><circle cx="420" cy="300" r="210" fill="#51e6ff" opacity=".18"/><text x="80" y="320" fill="#fff7ea" font-family="Georgia" font-size="132">VOLPONI</text><text x="86" y="390" fill="#f2c76b" font-family="Arial" font-size="34" letter-spacing="8">INTELIGÊNCIA EM ESTADO RARO</text></svg>`);
if (existsSync('public/favicon.ico')) copyFileSync('public/favicon.ico', join(dist, 'favicon.ico'));
if (existsSync(`public${content.image}`)) {
  mkdirSync(join(dist, 'lovable-uploads'), { recursive: true });
  copyFileSync(`public${content.image}`, join(dist, content.image));
}
console.log('VOLPONI rebuilt with shared content, long-form sections and static routes.');
