name: Unified Autopilot (Resolve + Fix + Merge + Deploy)

on:
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  checks: read
  pages: write
  id-token: write

jobs:
  pr-autopilot:
    if: github.event_name == 'pull_request_target'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout base repository (safe context)
        uses: actions/checkout@v4

      - name: Guard pull_request_target execution
        id: pr_guard
        env:
          PR_HEAD_REPO_FULL_NAME: ${{ github.event.pull_request.head.repo.full_name }}
          PR_AUTHOR_ASSOCIATION: ${{ github.event.pull_request.author_association }}
        run: ./scripts/guard-pull-request-target.sh

      - name: Checkout PR commit (trusted only)
        if: steps.pr_guard.outputs.allow == 'true'
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Setup Node
        if: steps.pr_guard.outputs.allow == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Configure git
        if: steps.pr_guard.outputs.allow == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Try auto-resolve conflicts with base branch
        if: steps.pr_guard.outputs.allow == 'true'
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
        run: |
          set +e
          git fetch origin "${BASE_REF}" --depth=1
          AUTO_RESOLVE_CODE=true ./scripts/auto-resolve-conflicts.sh "${BASE_REF}"
          RESOLVE_STATUS=$?
          set -e
          if [ "$RESOLVE_STATUS" -ne 0 ]; then
            echo "auto_resolve_failed=true" >> "$GITHUB_ENV"
          fi

      - name: Install and auto-fix
        if: steps.pr_guard.outputs.allow == 'true' && env.auto_resolve_failed != 'true'
        run: |
          npm ci
          npm run lint -- --fix
          npm run typecheck
          npm run build

      - name: Commit auto-fixes
        if: steps.pr_guard.outputs.allow == 'true' && env.auto_resolve_failed != 'true'
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git add -A
            git commit -m "chore: auto-resolve conflicts and auto-fix PR"
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi

      - name: Merge PR automatically
        if: steps.pr_guard.outputs.allow == 'true' && env.auto_resolve_failed != 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = context.payload.pull_request;
            if (!pr) return;
            await github.rest.pulls.merge({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
              merge_method: 'squash'
            });


      - name: Comment when guarded execution is blocked
        if: steps.pr_guard.outputs.allow != 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = context.payload.pull_request;
            if (!pr) return;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr.number,
              body: `🛡️ Unified Autopilot bloqueado por política de segurança: ${process.env.GUARD_REASON}.`
            });
        env:
          GUARD_REASON: ${{ steps.pr_guard.outputs.reason }}

      - name: Comment when auto-resolve fails
        if: steps.pr_guard.outputs.allow == 'true' && env.auto_resolve_failed == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = context.payload.pull_request;
            if (!pr) return;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr.number,
              body: '⚠️ A automação tentou resolver conflitos automaticamente e não conseguiu concluir. Reexecute o workflow após atualizar a base ou ajuste manual mínimo nos arquivos indicados em conflito.'
            });

  deploy-main:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install and verify
        run: |
          npm ci
          npm run ci:verify
      - name: Build Pages artifact
        env:
          VITE_BASE_PATH: /aix8c-site/
        run: |
          npm run build
          cp dist/index.html dist/404.html
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - name: Deploy pages
        id: deployment
        uses: actions/deploy-pages@v4
